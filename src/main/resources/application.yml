spring:
  datasource:
    url: jdbc:mysql://localhost:3306/coupang
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    session:
      host: localhost
      port: 6379

mybatis:
  type-aliases-package: com.coupang.marketplace.*.domain
  mapper-locations: mybatis/mapper/**/*.xml