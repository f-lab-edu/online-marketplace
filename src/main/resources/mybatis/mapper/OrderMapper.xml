<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.coupang.marketplace.order.repository.OrderRepository">
    <insert id="insertOrderInfo" parameterType="Order">
        INSERT INTO ORDERS (user_id, consumer_name, consumer_phone, receiver_name, receiver_address, receiver_phone, receiver_request, status, payment_id)
        VALUES (#{userId}, #{consumerName}, #{consumerPhone}, #{receiverName}, #{receiverAddress}, #{receiverPhone}, #{receiverRequest}, #{status}, #{paymentId})
        <selectKey keyProperty="id" resultType="long">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>

    <insert id="insertOrderProducts" parameterType="OrderProduct">
        INSERT INTO ORDER_PRODUCT (order_id, product_id, product_num)
        VALUES (#{orderId}, #{productId}, #{productNum})
    </insert>
</mapper>